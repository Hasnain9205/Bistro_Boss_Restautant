/**
 * 1. install stripe and stripe react; 
 * 2. create card elements;
 * 3. Create stripe account and get Publishable key;
 * 4. use publishable key and use stripe to get card information and error;
 * 5. create payment intent post on the server and return on the client Secret;
 * 6. install stripe on the server side and get client side,make sure you used the payment method types: ['card']
 * 7. from client side get the client secret and saved it ;
 * 8. use confirm card payment and pass user information,card and client secret;
 * 9.display transaction id.
 */